#!/bin/bash
cd "$(dirname "$0")"
echo "========================================"
echo "   開始同步 iCHEF 資料到 Google Sheets..."
echo "========================================"
echo ""

# 使用專案中的 python 環境執行 (如果有 venv) 或是系統 python
# 這裡假設使用者已經安裝了依賴
python3 sync_service.py

echo ""
echo "========================================"
echo "   同步完成！ (請檢查上方是否有錯誤訊息)"
echo "========================================"
echo "您可以關閉此視窗了"
read -n 1 -s -r -p "按任意鍵繼續..."
